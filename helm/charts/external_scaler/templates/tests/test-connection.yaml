apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "external_scaler.fullname" . }}-test-connection"
  labels:
    {{- include "external_scaler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "external_scaler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
